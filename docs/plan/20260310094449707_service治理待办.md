# Service 治理待办

## 下一阶段
- [x] 收敛 `crates/service/src/gateway/observability/http_bridge/stream_readers/*` 的 `use super::*`，改成显式依赖导入。
- [x] 继续细化 `crates/service/src/gateway/protocol_adapter/response_conversion/*`，把事件聚合、SSE 桥接、JSON 还原的可见性边界收稳。
- [x] 清理 `crates/service/src/app_settings/env_overrides/*` 中剩余的跨层访问，补一层明确门面。
- [x] 评估并开始收短 `crates/service/src/lib.rs` 的根级 `#[path]` 长清单，已完成 `usage`、`auth`、`requestlog`、`account`、`apikey` 子域门面化。
- [x] 继续收 `crates/service/src/lib.rs` 剩余根层模块中的 `storage`、`bootstrap/startup/shutdown`、`process_env/lock_utils/reasoning_effort`，分别归入 `storage/`、`lifecycle/`、`runtime/`。
- [x] 清理 `gateway/protocol_adapter/request_mapping/*` 与相关测试中的 wildcard 导入。
- [x] 继续收 `crates/service/src/lib.rs` 余下根层模块中的 `error_codes`、`rpc_auth`、`web_access`，分别归入 `errors/` 与 `auth/`。
- [x] 继续收 `crates/service/src/lib.rs` 余下根层模块，已完成 `rpc_dispatch` 的子域边界评估，当前保持目录门面形态不再继续拆分。
- [x] 将 `gateway/observability/http_bridge.rs` 与 `gateway/protocol_adapter.rs` 改为对应目录下的 `mod.rs`，消除“文件入口 + 子目录实现”混搭。
- [x] 已完成 `gateway/observability/http_bridge/mod.rs` 与 `gateway/protocol_adapter/mod.rs` 的根入口职责评估；当前门面已足够薄，暂不继续拆成 facade + implementation 双层。
- [x] 已完成 `gateway/protocol_adapter` 的二次收口：共享契约迁入 `types.rs`，请求入口迁入 `request_router.rs`，runtime reload 从根门面移走。
- [x] 已完成 `crates/service/tests/*` 共享支持层评估；当前仅 `gateway_logs/support.rs` 具备独立共享价值，暂不统一抽象。

## 验收口径
- [x] 每完成一个子域后重新执行 `cargo test -p codexmanager-service --no-run`。
- [x] 网关相关子域完成后执行 `cargo test -p codexmanager-service --test gateway_logs -- --nocapture`。
- [x] 设置相关子域完成后执行 `cargo test -p codexmanager-service --test app_settings -- --nocapture`。
- [x] 协议适配子域完成后执行 `cargo test -p codexmanager-service protocol_adapter -- --nocapture`。
- [x] 使用目录门面替代临时 `#[path]` 拼装时，根文件只保留模块声明与导出，不再承载业务实现。
- [x] 生命周期/运行时子域完成后执行 `cargo test -p codexmanager-service --test shutdown_flag -- --nocapture`、`cargo test -p codexmanager-service --test default_addr -- --nocapture`、`cargo test -p codexmanager-service --test rpc -- --nocapture`。
